spring:
  application:
   name: sadir-bootstrap
  datasource:
    url: jdbc:h2:mem:mydb
    username: sadir
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update